<!DOCTYPE html>
<html lang="en">
<head>
  <!-- Essential Meta -->
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <!-- <title><%= title ? `${title} | BuildSource` : 'BuildSource — Construction & Renovation Platform' %></title> -->
  <meta name="description" content="BuildSource is your all-in-one construction and renovation platform — connecting homeowners, contractors, and suppliers for a seamless building experience." />
  <meta name="keywords" content="BuildSource, construction, renovation, building materials, contractors, home improvement" />
  <meta name="author" content="BuildSource Team" />

  <!-- Favicon -->
  <link rel="icon" type="image/png" href="/assets/logo.png" />

  <!-- Open Graph / Facebook -->
  <meta property="og:title" content="<%= title || 'BuildSource — Construction & Renovation Platform' %>" />
  <meta property="og:description" content="Find trusted contractors, source quality materials, and manage your projects all in one place with BuildSource." />
  <meta property="og:image" content="/assets/logo.png" />
  <meta property="og:url" content="https://buildsource.in" />
  <meta property="og:type" content="website" />

  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="<%= title || 'BuildSource' %>" />
  <meta name="twitter:description" content="Reliable. Transparent. Local. BuildSource connects you to the best in construction and renovation." />
  <meta name="twitter:image" content="/assets/logo.png" />

  <!-- Fonts & Icons -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <script src="/js/fontawesome.min.js"></script>

  <!-- Styles -->
  <link rel="stylesheet" href="/css/styles.css" />
  <link rel="stylesheet" href="/css/header.css" />
  <link rel="stylesheet" href="/css/footer.css" />
  <% if (title === 'Hire Professionals') { %>
    <link rel="stylesheet" href="/css/hire.css" />
  <% } %>
  <% if (title === 'Materials') { %>
    <link rel="stylesheet" href="/css/materials.css" />
  <% } %>
  <% if (title === 'Contact') { %>
    <link rel="stylesheet" href="/css/contact.css" />
  <% } %>
  <% if (title === 'Pricing') { %>
    <link rel="stylesheet" href="/css/pricing.css" />
  <% } %>
  <% if (title === 'Services') { %>
    <link rel="stylesheet" href="/css/services.css" />
  <% } %>
  <% if (title === 'Projects') { %>
    <link rel="stylesheet" href="/css/projects.css" />
  <% } %>
  <% if (typeof css !== 'undefined') { %>
    <link rel="stylesheet" href="/css/<%= css %>" />
  <% } %>

  <!-- Theme Color for Mobile -->
  <meta name="theme-color" content="#f7a600" />
</head>
<body class="dark" style="font-family: 'Poppins', sans-serif;">
